# Acceptance Criteria

## Inputs
- stories: `workspace\factory_runs\global_supervisor_complex_trial_fixed\03_epics_stories.md`

## Criteria
- AC01: `create default world` is implemented with a local, repeatable check.
- AC02: `advance one tick` is implemented with a local, repeatable check.
- AC03: `choose automatic person action` is implemented with a local, repeatable check.
- AC04: `apply action effects` is implemented with a local, repeatable check.
- AC05: `summarize world state` is implemented with a local, repeatable check.
- AC06: `save and load world state` is implemented with a local, repeatable check.
- AC07: `run terminal demo` is implemented with a local, repeatable check.
- AC80: All explicitly requested files exist in the target project folder.
- AC90: Automated validation runs and reports a clear success marker or passing result.
- AC91: The final answer names validation evidence and current limits.
- AC92: No out-of-scope repository files are modified without an explicit reason.

## Gate Rule
No technical design or pattern decision may start until these criteria exist.
