# Tiny Planner App

This fixture exists for Repo Understanding Lab smoke tests.

The app entrypoint is `src/app.py`. It creates a `PlannerAgent`, asks it to
plan a task, and formats the result for CLI output.

